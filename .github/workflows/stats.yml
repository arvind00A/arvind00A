name: Generate GitHub Stats Cards

on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Create dist folder
        run: mkdir -p dist

      - name: Download Stats Card
        run: |
          curl -o dist/stats.svg "https://github-readme-stats.vercel.app/api?username=arvind00a&show_icons=true&theme=tokyonight&hide_border=true&bg_color=0D1117&title_color=7c3aed&icon_color=7c3aed&text_color=c9d1d9&count_private=true"

      - name: Download Top Languages Card
        run: |
          curl -o dist/top-langs.svg "https://github-readme-stats.vercel.app/api/top-langs/?username=arvind00a&layout=compact&theme=tokyonight&hide_border=true&bg_color=0D1117&title_color=7c3aed&text_color=c9d1d9"

      - name: Download Streak Card
        run: |
          curl -o dist/streak.svg "https://streak-stats.demolab.com?user=arvind00a&theme=tokyonight&hide_border=true&background=0D1117&ring=7c3aed&fire=7c3aed&currStreakLabel=7c3aed"

      - name: Generate Snake Animation
        uses: Platane/snk@v3
        with:
          github_user_name: arvind00a
          outputs: |
            dist/snake.svg
            dist/snake-dark.svg?palette=github-dark
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Commit and push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add dist/
          git diff --staged --quiet || git commit -m "chore: update stats cards"
          git push
